apiVersion: v1
kind: Pod
metadata:
  name: test-connection
spec:
  containers:
    - name: curl
      image: curlimages/curl
      args:
        - "curl"
        - "http://{{ .Values.service.host }}:{{ .Values.service.port }}"
